{
  "name": "@prompt-royale/room-do",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate-types": "wrangler types",
    "typecheck": "npm run generate-types && tsc --noEmit"
  }
}
